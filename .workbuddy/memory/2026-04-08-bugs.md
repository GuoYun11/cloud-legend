

## 2026-04-08 第二轮修复

### Bug修复：
1. **事件处理中this指向错误** - main.js第250行 `this._createCharacter` → `Game._createCharacter`
2. **Random.uid()函数不存在** - CombatSystem.js中`Random.uid()` → `Random.uuid()`
3. **SaveSystem未保存战斗状态** - 添加COMBAT_STATE保存逻辑

### 修改的文件：
- scripts/main.js：修复事件处理this指向
- systems/CombatSystem.js：修复Random.uid() → Random.uuid()
- systems/SaveSystem.js：添加战斗状态保存
